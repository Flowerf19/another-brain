# Archived Plans

Các tài liệu trong thư mục này là kế hoạch kiến trúc Redis/Docker-era đã bị
supersede cho nhánh `v0.11.0`. Chúng chỉ được giữ để tra cứu lịch sử và đối
chiếu legacy oracle; không dùng làm contract triển khai mới.

Canonical documents hiện hành:

- `../another-brain-architecture.md` — kiến trúc đã duyệt;
- `../07-multiplatform-embedded-runtime.md` — kế hoạch triển khai đang active.

Legacy implementation đầy đủ vẫn nằm trên `main` tại baseline `edc0e57`.

## Plan 07 completed sub-plans (`07/`)

`07/01`..`07/09` are the completed phase boards of the active rebuild plan
(contracts/oracle, package shell, clean-slate deletion, evidence harnesses,
embedding subsystem, SQLite storage, retrieval, service+MCP, import+cutover).
Archived 2026-08-06 — every task ticked with landed notes; kept as the
detailed execution record. Still active: `../../07-multiplatform-embedded-runtime.md`
(master board) and `../../07/10-release-gate.md` (GOAL-016, pending).
